/**
 * Template stub: main game plays UI SFX here. Minigame dashboard examples call `.play()`
 * on the returned ref; no-op keeps the bundle free of mandatory audio assets.
 */
export function useSound(_key: string): { play: () => void } {
  return { play: () => {} };
}
